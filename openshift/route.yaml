apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: welcome-api
  labels:
    app: welcome-api
spec:
  to:
    kind: Service
    name: welcome-api
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
